# Contributing

Thank you for helping improve Codex Usage Companion.

1. Search existing issues before opening a new one.
2. Keep changes focused and avoid adding analytics or remote services.
3. Add or update tests for behavior changes.
4. Run `npm run check`, `npm test`, and `cargo test` before submitting a pull request.
5. Never commit authentication files, account exports, complete email addresses,
   browser data, `.env` files, or raw App Server responses from real accounts.

Bug reports should include the app version, operating system, Codex CLI version,
expected behavior, and redacted error text.
